# ============================================================================
# SHEEP AI v3.0.0 - Standalone Configuration
# ============================================================================
# Copy to .env and fill in your values:
#   cp .env.example .env
# ============================================================================

# ---------------------------------------------------------------------------
# REQUIRED: Telegram Bot Token (from @BotFather on Telegram)
# ---------------------------------------------------------------------------
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# ---------------------------------------------------------------------------
# Claude Max API Proxy (PRIMARY LLM - uses your Max Plan, $0/token)
# ---------------------------------------------------------------------------
# Install: npm install -g claude-max-api-proxy
# Run: claude-max-api (starts on localhost:3456)
# Ensure Claude Code CLI is authenticated: claude --version
CLAUDE_PROXY_URL=http://localhost:3456/v1

# ---------------------------------------------------------------------------
# Anthropic API Key (FALLBACK - used only if proxy is down, pay-per-token)
# ---------------------------------------------------------------------------
# Get from: https://console.anthropic.com/
# Optional but recommended for reliability
ANTHROPIC_API_KEY=sk-ant-...

# ---------------------------------------------------------------------------
# Google AI API Key (for Gemini embeddings + Flash classification)
# ---------------------------------------------------------------------------
# Get from: https://aistudio.google.com/apikey
# Required for semantic search and fast prefetch classification
GOOGLE_AI_API_KEY=AI...

# ---------------------------------------------------------------------------
# SHEEP Configuration
# ---------------------------------------------------------------------------
SHEEP_AGENT_ID=default
SHEEP_DEBUG=false
PORT=3000

# Auto-consolidation: "disabled", "idle", or "scheduled"
SHEEP_AUTO_CONSOLIDATE=idle

# Idle threshold before triggering consolidation (minutes)
SHEEP_IDLE_THRESHOLD_MINUTES=120

# ---------------------------------------------------------------------------
# Model Overrides (optional - defaults to optimal 4-tier strategy)
# ---------------------------------------------------------------------------
# SHEEP_BRAIN_MODEL=claude-opus-4       # Opus 4.6 via proxy
# SHEEP_MUSCLE_MODEL=claude-sonnet-4    # Sonnet 4 via proxy
# SHEEP_REFLEX_MODEL=claude-haiku-4     # Haiku 4 via proxy
# SHEEP_LIGHTNING_MODEL=gemini-2.5-flash # Gemini Flash external

